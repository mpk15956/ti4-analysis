# Methodological Note: Anomaly Pathing and Asteroid Field Impassability

## The Constraint
In evaluating the spatial balance of a *Twilight Imperium IV* (TI4) map, the optimizer calculates the accessibility of planets based on their topological distance from a player's starting Home System. Anomalies alter these routing costs.

In this model's configuration (`DISTANCE_MOD_ASTEROID_FIELD = False`), Asteroid Fields are strictly defined as impassable barriers, completely severing the topological path between two hexes in the same manner as a Supernova.

## The Mathematical Tension
In spatial optimization and search-based procedural content generation (SBPCG), defining a node as strictly impassable introduces a mathematical "cliff" into the fitness landscape. If a random mutation places an Asteroid Field directly between a Home System and a high-value planet, the pathing algorithm fails, the distance approaches infinity, and the planet's evaluated value drops instantly to `0.0`. 

A common heuristic alternative is to assign a "soft" continuous penalty (e.g., adding `+1` to the pathing distance). This mathematically smooths the search space, allowing gradient-free metaheuristics to evaluate the map more efficiently without creating disconnected topological islands. 

## Academic Justification for Strict Impassability
Despite the increased ruggedness of the search space, this model rejects the heuristic "+1 penalty" approach in favor of strict impassability for the following reasons:

### 1. Strict Rules Adherence
The TI4 rulebook explicitly states: *"A ship cannot move through or into an asteroid field."* While the game allows players to eventually bypass this rule by acquiring the "Antimass Deflectors" technology, a baseline spatial model must evaluate the board's objective geography, not the subjective, conditional game states that occur later in the game.

### 2. The "Round 1" Draft Baseline
Competitive TI4 map drafting (such as Milty Draft) relies heavily on evaluating a slice's immediate economic viability in the first two rounds of the game. If an Asteroid Field blocks a player's expansion path, the planets behind it are functionally inaccessible for Round 1. Evaluating that path as strictly broken accurately reflects the severe economic and tempo loss a player faces.

### 3. Minimizing Assumptions (Faction-Agnostic Modeling)
Assigning a continuous distance penalty to Asteroid Fields mathematically assumes that *every* player will successfully research Antimass Deflectors. This introduces an unprovable assumption and biases the map generation toward factions that naturally start with Blue technology. Because the spatial optimizer utilizes blank placeholder Home Systems, it is explicitly designed to be faction-agnostic. Therefore, the algorithm cannot assume a player possesses the required technology to traverse the anomaly. 

## Impact on the Optimizer
By modeling Asteroid Fields as strict impassable barriers, the optimizer heavily penalizes maps that feature restrictive anomaly chokepoints near Home Systems. Any map that isolates a player behind an Asteroid Field will result in a localized resource score of zero, severely degrading the map's Multi-Jain Fairness Index and forcing the algorithm to aggressively reject the configuration. This ensures that the final maps generated by the algorithm are topologically equitable for all factions, regardless of their starting technology.

# Methodological Note: Nebula Logistical Transparency in Round 0

## The Configuration
In this model, Nebulas are assigned a distance modifier of `0.0` (`DISTANCE_MOD_NEBULA = 0.0`). Unlike Asteroid Fields, which are modeled as strictly impassable to reflect their "Hard Block" nature in a no-technology state, Nebulas are treated as logistically transparent.

## The "Move-1 Carrier" Baseline
A critical pillar of this research is the **Strict Round 0 Baseline**, which assumes players possess no technology or faction-specific movement bonuses. Under these conditions, the primary units capable of capturing planets—Carriers and Dreadnoughts—possess a base **Move value of 1**.

### Rule Synchronicity:
1. **Movement into a Nebula:** A ship may move into a nebula but must stop.
2. **Movement out of a Nebula:** A ship starting in a nebula treats its move value as 1.

For a Move-1 unit, these restrictions are redundant. A Carrier moving into an adjacent nebula stops after one hex (its maximum range) and moves one hex when exiting (its standard move). Consequently, the Nebula introduces **zero additional logistical friction** for Round 1 expansion when compared to empty space.

## Comparison to Heuristic "Speed Bump" Models
While many community models apply a `+1.0` distance penalty to Nebulas, such models implicitly assume a **Move-2 Baseline** (e.g., ships with Gravity Drive or Cruiser IIs). In a Move-2 scenario, a Nebula forces a "stop," consuming an extra tactical action and adding one unit of logistical effort. 

By rejecting this penalty and using `0.0`, our model remains intellectually consistent with the **Round 0 / Faction-Agnostic** methodology:
* **Asteroid Fields = False:** Strictly impassable without tech (Round 0 Hard Block).
* **Nebulas = 0.0:** Fully passable by Move-1 units without penalty (Round 0 Transparency).

## Impact on Optimization
This choice ensures that the spatial optimizer does not unfairly penalize slices containing Nebulas. It correctly identifies that, for the purpose of initial geographic resource distribution, a Nebula does not impede a player's baseline capacity to reach and capture their immediate

# Methodological Note: Nebula Logistical Transparency in Round 0

## The Configuration
In this model, Nebulas are assigned a distance modifier of `0.0` (`DISTANCE_MOD_NEBULA = 0.0`). This contrasts with the treatment of Asteroid Fields, which are modeled as strictly impassable (`False`) to reflect a "Hard Block" in a no-technology state.

## The "Capture-Capable Unit" Constraint
The optimizer evaluates the spatial distribution of map resources based on the accessibility of economic resources (resources, influence) and technology skips. In *Twilight Imperium IV*, capturing these assets requires units with **Capacity** to transport ground forces.

### Base Unit Stats (Round 0):
* **Carrier**: Move 1, Capacity 4
* **Dreadnought**: Move 1, Capacity 1
* **Cruiser/Destroyer**: Move 2, Capacity 0 (Ineligible for independent capture)

Under the **Strict Round 0 Baseline** (no technology assumed), the functional units being modeled have a **Movement value of 1**.

## Rule Synchronicity and Transparency
According to the official rules, Nebulas impose two primary movement restrictions:
1. **Compulsory Stop**: A ship moving into a nebula must stop.
2. **Move Limit**: A ship starting in a nebula treats its move value as 1.

For a Move-1 unit, these restrictions are functionally transparent. A Carrier takes one tactical action to enter an adjacent nebula and one tactical action to exit. This is identical to the logistical effort required to move through empty space. Therefore, adding a distance penalty (+1.0) would incorrectly over-penalize the slice's early-game value.

## Conclusion
By setting the modifier to `0.0`, the model remains strictly rule-adherent to the Round 0 state. It avoids making heuristic assumptions about mid-game technology (e.g., Move-2 upgrades) and focuses purely on the initial geographic resource distribution of the map as it exists at the beginning of the game.

# Methodological Note: Gravity Rift Risk-Reward Modeling

## The "Expected Effort" Proxy
This model replaces the probabilistic nature of Gravity Rift rolls (30% destruction, 70% movement bonus) with a deterministic "Expected Effort" modifier. This ensures the optimizer remains computationally efficient while accurately reflecting the strategic value of the anomaly.

## The Mathematical Model
The distance modifier ($M$) for a Gravity Rift is calculated as a function of its distance from the player's Home System ($d_{rift}$), measured in hops along the path:
$$M = 0.3d_{rift} - 0.4$$

This formula is derived from the expected value of logistical effort, assuming that a destroyed ship must be replaced and re-traversed from the Home System ("Restart from Home" premise). After a failed rift roll, the model assumes the player uses a **non-rift recovery path** (Risk-Averse Baseline): we do not assume re-risking the rift, because the marginal utility of a second successful rift roll is outweighed by the catastrophic tempo cost of a second ship loss in Round 0.

### Inflection Point Example
The formula yields a **dynamic inflection point** where the anomaly shifts from strategic asset to liability:

| $d_{rift}$ | $M$   | Interpretation |
|------------|-------|----------------|
| 1          | $-0.1$ | Net benefit (faster expected reach) |
| 2          | $+0.2$ | Net penalty |
| 3          | $+0.5$ | Stronger penalty |

So a rift adjacent to home ($d_{rift}=1$) is a net benefit; rifts two or more hops away are net penalties whose severity grows with distance.

## Capacity Loss Abstraction
Losing a carrier in a Gravity Rift also loses any transported ground forces and fighters. The model does not track unit inventories explicitly; planet value is a proxy for economic opportunity. Because the distance-decay matrix already penalizes systems that take longer to reach, the increased effective distance from the Expected Effort modifier ($M$) is a mathematically consistent proxy for the lost opportunity to capture those planets in the same time window.

## Academic Justification
1. **Tempo as a Finite Resource**: In competitive *Twilight Imperium IV*, early-game unit loss is primarily a penalty of "tempo" (time and command tokens). Re-traversing from home accurately models the action-economy cost of recovery.
2. **Dynamic Inflection Points**: This model captures the shift in strategic value based on geography (see table above).
3. **Static Topology Integration**: Since map anomalies are frozen during optimization, the distance from each home seat to a rift is constant. The modifier is applied when computing shortest modified distance along each path, preserving O(1) evaluation speed in the metaheuristic once the topology is built.
4. **Optionality of the Rift**: The pathfinding algorithm uses breadth-first search to minimize modified distance. It therefore automatically chooses between a safe path and a rift path: the effective cost is $\min(\text{safe distance},\ \text{rift path distance} + M)$. Rational players take the rift only when expected tempo favors it.

---

# Optimization-Run Anomaly: SA Seed=37 Temperature-Calibration Failure (Phase 2b)

## The Observation
In the Phase 2b methods-justification benchmark (six algorithms × 100 seeds × eight evaluation budgets, `output/saturation_20260314_205919/benchmark_20260315_074617/`), one SA run — **seed = 37**, accounting for ≈1% of SA's Phase 2b observations — failed to escape its initial configuration. The optimizer recorded a final composite of **0.1816** with zero accepted moves across all evaluation budgets, on a starting map with **initial Moran's I = +0.439** (positive autocorrelation, atypical of the seed pool).

## Mechanism
SA's initial temperature $T_0$ is calibrated by sampling $\Delta$-proposals from the starting configuration and choosing $T_0$ such that the average acceptance rate matches a target (default 80%, see `scripts/optimize_hyperparameters.py` for the tuned `initial_acceptance_rate = 0.788681`). When the starting configuration is itself a deep local minimum on the *spatial* axis — a high-clustering arrangement near the boundary of feasible permutations — sampled $\Delta$ values are dominated by unfavorable moves, producing a $T_0$ small enough that effectively no proposal clears the Metropolis criterion. The chain then never moves. This is a known small-sample failure mode of acceptance-rate-targeted temperature calibration; see Atiqullah (2004) and standard SA literature on cold-start pathologies.

## Scope of the Effect
- **Other algorithms handle seed=37 normally.** HC achieves composite = 0.0002, SGA, NSGA-II, and TS all escape the initial state without anomaly. The failure is specific to SA's $T_0$ heuristic, not to the seed itself.
- **Phase 1 results are not affected.** Phase 1 (the five-condition ablation, `benchmark_20260314_233002/`) uses three independent SA chains per seed and the *minimum* composite is reported. With three chains, the probability of all three sharing the same calibration failure on seed=37 is negligible.
- **Phase 2b headline rankings are unaffected.** The pairwise Wilcoxon results comparing SA to other algorithms remain significant ($p_{\text{corr}} < 0.001$, `pairwise_tests.csv`) with or without seed=37 included.

## Mitigation in Reported Results
Per the pre-registered protocol, no observations are excluded from the published Phase 2b summary table. The seed=37 SA composite contributes to the long upper tail visible in the SA distribution (`fig_summary.pdf`) but does not perturb the median or 25th percentile, which are the values cited in the algorithm-comparison narrative. Future work should re-calibrate $T_0$ from a *secondary* configuration when the primary configuration's $\Delta$-distribution is degenerate; this is a one-line change in `simulated_annealing.py` and is left for a follow-up release rather than retroactively re-running the benchmark.